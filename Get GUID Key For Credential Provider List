Get-ChildItem 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Authentication\Credential Providers' |
ForEach-Object {
    $key = $_.PSChildName
    $name = (Get-Item $_.PSPath).GetValue('')
    [PSCustomObject]@{
        GUID = $key
        Name = $name
    }
} | Format-Table -AutoSize
